<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Expense</title>
    <link href="/remix/remixicon.css" type="text/css" rel="stylesheet" />
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background: linear-gradient(to bottom, #f5f5f5, #ddd);
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .update-container {
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 400px;
            text-align: center;
        }

        .update-container h2 {
            color: #573b8a;
            margin-bottom: 15px;
        }

        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }

        .form-group label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .form-group input[type="text"],
        .form-group input[type="number"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            outline: none;
        }

        .form-group input[type="submit"] {
            width: 100%;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background: #573b8a;
            color: #fff;
            cursor: pointer;
            font-weight: bold;
        }

        .form-group input[type="submit"]:hover {
            background: #482c72;
        }

        .back-link {
            display: block;
            margin-top: 10px;
            text-decoration: none;
            color: #573b8a;
        }

        .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <div class="update-container">
        <h2>Update Expenses</h2>
        <form action="/budget/editExpense" method="post">
            <input type="hidden" name="eid" value="<%= updateExpense._id %>">
            <div class="form-group">
                <label for="title">Expense Title:</label>
                <input type="text" id="title" name="title" value="<%= updateExpense.title %>" required />
            </div>
        
            <div class="form-group">
                <label for="price">Expense Price:</label>
                <input type="number" id="price" name="price" value="<%= updateExpense.price %>" required />
            </div>
        
            <div class="form-group">
                <input type="submit" value="Update Expense">
            </div>
        </form>
        
        <a href="/budget" class="back-link">Back to Budget</a>
    </div>

</body>

</html>
